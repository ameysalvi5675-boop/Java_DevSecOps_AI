apiVersion: v1
kind: Service

metadata:
  name: product-service
  namespace: devsecops-ai

spec:
  type: NodePort

  selector:
    app: product-service

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081