apiVersion: v1
kind: Service

metadata:
  name: cart-service
  namespace: devsecops-ai

spec:
  type: NodePort

  selector:
    app: cart-service

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30083